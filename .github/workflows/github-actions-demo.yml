name: GitHub Actions Demo
run-name: ${{ github.actor }} is testing out GitHub Actions 🚀
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: build and push a container image
      run: docker build -f Dockerfile.dev -t sehoon0519/docker-react-dev

        
